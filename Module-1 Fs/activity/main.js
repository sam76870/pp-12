let treeObj = require("./command/tree");
let organizeObj = require("./command/organize");
let viewObj = require("./command/help")
let inputArr = prcess.argv.slice(2);
let cmd = inputArr[0];
switch(cmd){
    case 'help':
        

}